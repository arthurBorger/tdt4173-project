# Copy to .env and replace these values with your Workbench configuration.
PROJECT_ID=your-gcp-project-id
# Use the instance's zone, for example us-west1-a.
LOCATION=us-west1-a
INSTANCE=your-workbench-instance-name
# Folder inside your OS Login home directory on the VM.
REMOTE_DIR=tdt4173-repo
